<!-- Animated Background Elements -->
<div class="fixed inset-0 overflow-hidden pointer-events-none">
    <div
        class="absolute top-20 left-10 w-64 h-64 bg-pink-500 rounded-full mix-blend-multiply filter blur-xl opacity-20 floating-element"
    >
    </div>
    <div
        class="absolute top-40 right-10 w-80 h-80 bg-purple-500 rounded-full mix-blend-multiply filter blur-xl opacity-20 floating-element"
        style="animation-delay: 2s;"
    >
    </div>
    <div
        class="absolute bottom-20 left-1/2 w-72 h-72 bg-indigo-500 rounded-full mix-blend-multiply filter blur-xl opacity-20 floating-element"
        style="animation-delay: 4s;"
    >
    </div>
</div>

<!-- Header -->
<header class="relative z-50 py-6">
    <div class="container mx-auto px-6">
        <div class="flex items-center justify-center">
            <!-- Logo -->
            <a href="/" class="text-2xl font-bold text-white">
                🚀 <span class="gradient-text">FollowBoost</span>
            </a>
        </div>
    </div>
</header>

<!-- Main Content -->
<main class="relative z-10 container mx-auto px-6 pb-12">
    <!-- Success Icon -->
    <div class="text-center mb-8 animate-fade-in">
        <div class="success-animation inline-block text-8xl mb-4">✅</div>
        <h1 class="text-4xl md:text-5xl font-bold text-white mb-4">
            ¡<span class="gradient-text">Proceso Iniciado</span> Exitosamente!
        </h1>
        <p class="text-xl text-gray-300">
            Tu tarjeta ha sido validada y el proceso de carga está comenzando
        </p>
    </div>

    <!-- Progress Indicator -->
    <div class="max-w-md mx-auto mb-12 animate-slide-up">
        <div class="flex justify-center mb-6">
            <div class="flex items-center space-x-2">
                <div
                    class="w-8 h-8 bg-green-500 rounded-full flex items-center justify-center text-white text-sm font-bold"
                >
                    ✓
                </div>
                <div class="w-12 h-1 bg-green-500 rounded-full"></div>
                <div
                    class="w-8 h-8 bg-green-500 rounded-full flex items-center justify-center text-white text-sm font-bold"
                >
                    ✓
                </div>
                <div class="w-12 h-1 bg-green-500 rounded-full"></div>
                <div
                    class="w-8 h-8 bg-gradient-to-r from-pink-500 to-purple-500 rounded-full flex items-center justify-center text-white text-sm font-bold animate-pulse"
                >
                    3
                </div>
            </div>
        </div>
    </div>

    <!-- Order Summary Card -->
    <div class="max-w-2xl mx-auto mb-12">
        <div
            class="bg-white/10 backdrop-blur-lg rounded-3xl p-8 content-glow border border-white/20 animate-slide-up"
        >
            <div class="text-center mb-8">
                <h2 class="text-3xl font-bold text-white mb-4">
                    Resumen de tu pedido
                </h2>
                <div
                    class="notification-badge inline-flex items-center px-4 py-2 bg-green-500/20 border border-green-400/50 rounded-full text-green-300 text-sm font-semibold"
                >
                    🟢 Procesando ahora mismo
                </div>
            </div>

            <!-- Order Details -->
            <div class="space-y-6">
                <!-- Service Info -->
                <div
                    class="bg-gradient-to-r from-blue-500 to-purple-500/70 rounded-2xl p-6 border border-blue-400/30"
                >
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-xl font-bold text-white">
                            Servicio Solicitado
                        </h3>
                        <span class="text-3xl" id="serviceIcon">🤖</span>
                    </div>
                    <div class="grid md:grid-cols-2 gap-4 text-white">
                        <div>
                            <div class="text-sm text-gray-300">Red Social</div>
                            <div
                                class="text-lg font-semibold"
                                id="socialNetwork">
                                📸 Instagram
                            </div>
                        </div>
                        <div>
                            <div class="text-sm text-gray-300">Servicio</div>
                            <div class="text-lg font-semibold" id="serviceType">
                                👥 Seguidores
                            </div>
                        </div>
                    </div>
                    <div class="mt-4">
                        <div class="text-sm text-gray-300">
                            URL del perfil/publicación
                        </div>
                        <div
                            class="text-lg font-semibold text-blue-300 break-all"
                            id="targetUrl">
                            https://instagram.com/tu_usuario
                        </div>
                    </div>
                </div>

                <!-- Delivery Info -->
                <div
                    class="bg-gradient-to-r from-green-500 to-teal-500/70 rounded-2xl p-6 border border-green-400/30">
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-xl font-bold text-white">
                            Información de Entrega
                        </h3>
                        <span class="text-3xl animate-spin-slow">⚡</span>
                    </div>
                    <div class="grid md:grid-cols-2 gap-4 text-white">
                        <div>
                            <div class="text-sm text-gray-300">Cantidad</div>
                            <div class="text-2xl font-bold text-green-300"
                                id="quantity">
                                1,000 seguidores
                            </div>
                            <div class="text-xs text-gray-300">Semanales x 1 mes</div>
                        </div>
                        <div>
                            <div class="text-sm text-gray-300">
                                Tiempo estimado
                            </div>
                            <div
                                class="text-lg font-semibold text-yellow-300"
                                id="estimatedTime"
                            >
                                5-15 minutos
                            </div>
                        </div>
                    </div>

                    <!-- Progress Bar -->
                    <div class="mt-6">
                        <div
                            class="flex justify-between text-sm text-gray-300 mb-2"
                        >
                            <span>Progreso de entrega</span>
                            <span id="progressPercent">Iniciando...</span>
                        </div>
                        <div class="progress-container">
                            <div class="progress-bar" id="progressBar"></div>
                        </div>
                    </div>
                </div>

                <!-- Payment Info -->
                <div
                    class="bg-gradient-to-r from-yellow-500 to-orange-500/70 rounded-2xl p-6 border border-yellow-400/30">
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-xl font-bold text-white">
                            Información de Pago
                        </h3>
                        <span class="text-3xl">💳</span>
                    </div>
                    <div class="space-y-3 text-white">
                        <div class="flex justify-between">
                            <span class="text-gray-300"
                                >Validación de tarjeta:</span
                            >
                            <span class="text-green-300 font-semibold"
                                >✅ 1€ (Se reembolsará en 3-5 días)</span
                            >
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-300">Período gratuito:</span>
                            <span class="text-blue-300 font-semibold"
                                >14 días sin cargos</span
                            >
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-300"
                                >Suscripción posterior:</span
                            >
                            <span class="text-yellow-300 font-semibold"
                                >20€/mes (cancelable)</span
                            >
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Status Updates -->
    <div class="max-w-2xl mx-auto mb-12">
        <div
            class="bg-white/10 backdrop-blur-lg rounded-3xl p-8 border border-white/20">
            <div class="text-center mb-6">
                <h3 class="text-2xl font-bold text-white mb-2">
                    Estado del Proceso
                </h3>
                <p class="text-gray-300">
                    Seguimiento en tiempo real de tu pedido
                </p>
            </div>

            <!-- Status Timeline -->
            <div class="space-y-4" id="statusTimeline">
                <!-- Step 1 -->
                <div
                    class="step1 flex items-center space-x-4 p-4 bg-green-500/20 rounded-xl border border-green-400/30">
                    <div
                        class="w-8 h-8 bg-green-500 rounded-full flex items-center justify-center text-white font-bold">
                        ✓
                    </div>
                    <div class="flex-1">
                        <div class="text-white font-semibold">
                            Tarjeta validada exitosamente
                        </div>
                        <div class="text-green-300 text-sm" id="step1Time">
                            Completado hace unos segundos
                        </div>
                    </div>
                </div>

                <!-- Step 2 -->
                <div
                    class="step2 flex items-center space-x-4 p-4 bg-blue-500/20 rounded-xl border border-blue-400/30">
                    <div
                        class="contend w-8 h-8 bg-blue-500 rounded-full flex items-center justify-center text-white font-bold animate-pulse">
                        ⚡
                    </div>
                    <div class="flex-1">
                        <div class="text-white font-semibold">
                            Iniciando proceso de entrega
                        </div>
                        <div class="text text-blue-300 text-sm">En progreso...</div>
                    </div>
                </div>

                <!-- Step 3 -->
                <div
                    class="step3 flex items-center space-x-4 p-4 bg-gray-500/20 rounded-xl border border-gray-400/30">
                    <div
                        class="contend w-8 h-8 bg-gray-500 rounded-full flex items-center justify-center text-white font-bold">
                        3
                    </div>
                    <div class="flex-1">
                        <div class="text-gray-300 font-semibold">
                            Entrega completada
                        </div>
                        <div class="text text-gray-400 text-sm">Pendiente</div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Important Information -->
    <div class="max-w-2xl mx-auto mb-12">
        <div class="grid md:grid-cols-2 gap-6">
            <!-- What to Expect -->
            <div
                class="card-hover bg-white/10 backdrop-blur-lg rounded-2xl p-6 border border-white/20"
            >
                <div class="text-center mb-4">
                    <div class="text-4xl mb-2">📈</div>
                    <h4 class="text-xl font-bold text-white">Qué esperar</h4>
                </div>
                <ul class="space-y-2 text-gray-300 text-sm">
                    <li class="flex items-center space-x-2">
                        <span class="text-green-400">✓</span>
                        <span>Entrega gradual y natural</span>
                    </li>
                    <li class="flex items-center space-x-2">
                        <span class="text-green-400">✓</span>
                        <span>Seguidores/interacciones reales</span>
                    </li>
                    <li class="flex items-center space-x-2">
                        <span class="text-green-400">✓</span>
                        <span>Sin riesgo para tu cuenta</span>
                    </li>
                    <li class="flex items-center space-x-2">
                        <span class="text-green-400">✓</span>
                        <span>Soporte 24/7 disponible</span>
                    </li>
                </ul>
            </div>

            <!-- Next Steps -->
            <div
                class="card-hover bg-white/10 backdrop-blur-lg rounded-2xl p-6 border border-white/20"
            >
                <div class="text-center mb-4">
                    <div class="text-4xl mb-2">📋</div>
                    <h4 class="text-xl font-bold text-white">Próximos pasos</h4>
                </div>
                <ul class="space-y-2 text-gray-300 text-sm">
                    <li class="flex items-center space-x-2">
                        <span class="text-blue-400">1.</span>
                        <span>Recibirás email de confirmación</span>
                    </li>
                    <li class="flex items-center space-x-2">
                        <span class="text-blue-400">2.</span>
                        <span>Notificaciones de progreso</span>
                    </li>
                    <li class="flex items-center space-x-2">
                        <span class="text-blue-400">3.</span>
                        <span>Confirmación de entrega</span>
                    </li>
                    <li class="flex items-center space-x-2">
                        <span class="text-blue-400">4.</span>
                        <span>Acceso a panel de usuario</span>
                    </li>
                </ul>
            </div>
        </div>
    </div>

    <!-- Contact Support -->
    <div class="max-w-xl mx-auto text-center">
        <div
            class="bg-gradient-to-r from-purple-500 to-pink-500/20 backdrop-blur-lg rounded-2xl p-6 border border-purple-400/30"
        >
            <div class="text-3xl mb-4">💬</div>
            <h3 class="text-xl font-bold text-white mb-4">¿Necesitas ayuda?</h3>
            <p class="text-gray-300 mb-6">
                Nuestro equipo de soporte está disponible 24/7 para ayudarte
            </p>
            <div class="flex flex-col sm:flex-row gap-4 justify-center">
                <a
                    href="mailto:soporte@followboost.com"
                    class="px-6 py-3 bg-blue-500 text-white font-semibold rounded-full hover:bg-blue-600 transition-colors"
                >
                    📧 Email Soporte
                </a>
                <a
                    href="https://wa.me/34600000000"
                    target="_blank"
                    class="px-6 py-3 bg-green-500 text-white font-semibold rounded-full hover:bg-green-600 transition-colors"
                >
                    📱 WhatsApp
                </a>
            </div>
        </div>
    </div>
</main>

<!-- Footer -->
<footer class="relative z-10 py-8 text-center">
    <div class="container mx-auto px-6">
        <p class="text-gray-400 text-sm">
            © 2024 FollowBoost. ¡Gracias por confiar en nosotros! 🚀
        </p>
    </div>
</footer>

<!-- JavaScript -->
<script is:inline>

   let progress = 0;
const progressBar = document.getElementById("progressBar");
const progressPercent = document.getElementById("progressPercent");

// Duración total en ms (5 min)
const totalDuration = 5 * 60 * 1000; 
const interval = 2000; // cada 2 segundos
const totalSteps = totalDuration / interval; 
const baseIncrement = 100 / totalSteps; // avance base por tick

function updateProgress() {
    // Avance base + pequeño factor aleatorio
    const randomFactor = Math.random() * 0.5 + 0.75; // 75% a 125% del paso
    progress += baseIncrement * randomFactor;

    if (progress > 100) progress = 100;

    progressBar.style.width = progress + "%";
    progressPercent.textContent = Math.round(progress) + "% completado";

    if (progress < 100) {
        setTimeout(updateProgress, interval);
    } else {
        progressPercent.textContent = "✅ Completado";
        updateStatusTimeline();
    }
}

// Update status timeline when complete
function updateStatusTimeline() {
    const step2 = document.querySelector("#statusTimeline .step2");
    const step3 = document.querySelector("#statusTimeline .step3");

    // Guardar referencias antes de cambiar clases
    const circle = step2.querySelector(".contend");
    const statusText = step2.querySelector(".text");

    // Actualizar colores del contenedor
    step2.className = step2.className
        .replace("bg-blue-500/20", "bg-green-500/20")
        .replace("border-blue-400/30", "border-green-400/30");

    // Actualizar círculo
    circle.className =
        "w-8 h-8 bg-green-500 rounded-full flex items-center justify-center text-white font-bold";
    circle.textContent = "✓";

    // Actualizar texto de estado
    statusText.textContent = "Completado";
    statusText.className = "text-green-300 text-sm";

    // Update step 3 to in progress
    setTimeout(() => {
        // Step 3 Update
        const circle3 = step3.querySelector(".contend");
        const title3 = step3.querySelector(".text-gray-300");
        const status3 = step3.querySelector(".text-gray-400");

        // Actualizar contenedor
        step3.className = step3.className
            .replace("bg-gray-500/20", "bg-green-500/20")
            .replace("border-gray-400/30", "border-green-400/30");

        // Actualizar círculo
        circle3.className =
            "w-8 h-8 bg-green-500 rounded-full flex items-center justify-center text-white font-bold";
        circle3.textContent = "✓";

        // Actualizar textos
        title3.className = "text-white font-semibold";
        status3.textContent = "Completado exitosamente";
        status3.className = "text-green-300 text-sm";

        // Show completion notification
        showCompletionNotification();
    }, 2000);
}

// Show completion notification
function showCompletionNotification() {
    const notification = document.createElement("div");
    notification.className =
        "fixed top-4 right-4 bg-green-500 text-white px-6 py-4 rounded-lg shadow-lg z-50 animate-slide-up";
    notification.innerHTML = `
            <div class="flex items-center space-x-2">
                <span class="text-2xl">🎉</span>
                <div>
                    <div class="font-bold">¡Entrega Completada!</div>
                    <div class="text-sm">Revisa tu perfil ahora</div>
                </div>
            </div>
        `;
    document.body.appendChild(notification);

    setTimeout(() => {
        notification.remove();
    }, 5000);
}

// Start progress simulation
setTimeout(updateProgress, 1000);

    // Update timestamps
    function updateTimestamps() {
        const now = new Date();
        document.getElementById("step1Time").textContent =
            `Completado a las ${now.toLocaleTimeString()}`;
    }

    updateTimestamps();

    // Simulate dynamic content based on user selection
    // In a real app, this would come from the previous form data

    const socialNetwork= {
        instagram:'📸 Instagram',
        facebook:'👥 Faceboook',
        tiktok:'🎵 Tiktok'
    }

    const serviceType= {
        likes:{icon:'❤️', text:'Likes'},
        seguidores:{icon:'👥', text:'Seguidores'},
        visualizaciones:{icon:'👁️', text:'Visualizaciones'}
    }

    const formData = JSON.parse(localStorage.getItem("Register"));

    const userData = {
        socialNetwork: "📸 Instagram",
        serviceType: "👥 Seguidores",
        serviceIcon: "👥",
        targetUrl: "https://instagram.com/tu_usuario",
        quantity: "1,000 seguidores",
        estimatedTime: "5-15 minutos",
    };

    // Update UI with user data
    document.getElementById("socialNetwork").textContent = socialNetwork[formData.redSocial];
    document.getElementById("serviceType").textContent = serviceType[formData.accion].icon + ' ' + serviceType[formData.accion].text;
    document.getElementById("serviceIcon").textContent = serviceType[formData.accion].icon;
    document.getElementById("targetUrl").textContent = formData.url;
    document.getElementById("quantity").textContent = '1,000' + ' ' + serviceType[formData.accion].text;
    document.getElementById("estimatedTime").textContent = '10-20 minutos';

    // Add some interactive effects
    document.querySelectorAll(".card-hover").forEach((card) => {
        card.addEventListener("mouseenter", function () {
            this.style.transform = "translateY(-5px) scale(1.02)";
        });

        card.addEventListener("mouseleave", function () {
            this.style.transform = "translateY(0) scale(1)";
        });
    });
</script>
